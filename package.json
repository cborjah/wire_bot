{
  "name": "voxel_bot",
  "version": "1.0.0",
  "description": "Sends messages to wire-earning channel on a set interval.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^13.6.0"
  }
}
